---
title: artifex.guardrail.train() | Tanaos Artifex
description: Learn how to use the .train() method to train a Guardrail model with the Artifex library.
keywords: [ artifex, tanaos, task specific llm, nlp, text classification, no training data, guardrail models, chatbot guardrail, train ] 
hide_table_of_contents: true
---

import Link from '@docusaurus/Link';
import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

import { APIDescriptionSection } from '../../src/components/APIDescriptionSection';
import { SYNTHEX_GENERATE_DATA_URL } from '../../consts';
import { ParameterTypeBadge, OptionalParameterBadge, DefaultParameterBadge } from '../../src/components/ParameterBadge';


<div className='responsive-container'>
	<div className='text-container'>
        # `guardrail.train()`

        Fine-tunes a pre-trained Guardrail model on user-specified instructions.
		If `train_dataset_path` is not provided, synthetic training data will be generated for the
		specified `unsafe_categories` and `language`. If `train_dataset_path` is provided, the model 
		will be trained on the dataset located at the specified path instead, and all arguments 
		related to synthetic data generation is ignored.

		Both the base Guardrail model [tanaos/tanaos-guardrail-v1](https://huggingface.co/tanaos/tanaos-guardrail-v1) 
		and previously trained models can be further trained using this method:
		- Fine-tune the base Guardrail model:
			```python
			Artifex().guardrail().train()
			```
		- Fine-tune a model that was previously trained with Artifex, in order to train it further: 
			```python
			Artifex().guardrail().load("trained/model/path").train()
			```

		<APIDescriptionSection title='Model Language'>
			You can specify the language of the pre-trained model that you want to fine-tune,
			by passing the `language` argument when initializing the `guardrail` object. 
			
			For example, if you want to fine-tune a model starting from a Spanish pre-trained model,
			you should initialize the `guardrail` object as follows:
			```python
			from artifex import Artifex

			guardrail = Artifex().guardrail(language="spanish")
			```

			the available languages are:

			- English (`language="english"`)
			- Spanish (`language="spanish"`)
			- German (`language="german"`)

			If no language is specified, `language="english"` will be used by default.
		</APIDescriptionSection>

		<APIDescriptionSection title='Arguments'>
			- **unsafe_categories** <ParameterTypeBadge>list[dict]</ParameterTypeBadge> <OptionalParameterBadge />\
				A list of dictionaries, where each dictionary describes a type of content that 
				should be classified as unsafe by the guardrail. Dictionary keys are the category 
				names (will appear as labels in the model output), and values are descriptions of 
				each category. If `train_dataset_path` is not provided, this argument is required. If 
				`train_dataset_path` is provided, this argument is ignored.
			- **language** <ParameterTypeBadge>str</ParameterTypeBadge> <OptionalParameterBadge /> <DefaultParameterBadge value='english' />\
				A string which specifies the language that the model will be trained to understand and perform inference in.
				If `train_dataset_path` is provided, this argument is ignored.
			- **output_path** <ParameterTypeBadge>str</ParameterTypeBadge> <OptionalParameterBadge />\
				A string which specifies the path where the output files will be generated. The output files consist of:
				- The training dataset
				- The output model `safetensor` and configuration files
			- **num_samples** <ParameterTypeBadge>int</ParameterTypeBadge> <OptionalParameterBadge /> <DefaultParameterBadge value='500' />\
				An integer which specifies the number of datapoints that the synthetic training dataset should consist of, and 
				that the model will be trained on. The maximum number of datapoints you can train your model on depends on 
				whether you are on a free or paid plan. If `train_dataset_path` is provided, this argument is ignored.
			- **num_epochs** <ParameterTypeBadge>str</ParameterTypeBadge> <OptionalParameterBadge /> <DefaultParameterBadge value='3' />\
				An integer which specifies the number of epochs to train the model for.
			- **device** <ParameterTypeBadge>int</ParameterTypeBadge> <OptionalParameterBadge />\
				A string which specifies the device to use. It can be one of the following:
				- `0` - Use the first GPU available
				- `1` - Use the second GPU available
				- ...
				- `-1` - Use the CPU
				
				If not specified, the method will automatically select the GPU if available, otherwise it will use 
				the CPU.
			- **disable_logging** <ParameterTypeBadge>bool</ParameterTypeBadge> <OptionalParameterBadge />\
				If set to `True`, disables inference metric logging for this call. By default, logging is enabled.
			- **train_dataset_path** <ParameterTypeBadge>str</ParameterTypeBadge> <OptionalParameterBadge />\
				A string which specifies the path to a custom training dataset to train the model on. If this argument is provided,
				synthetic data will not be generated, all arguments related to synthetic data generation (i.e. `language`, 
				`num_samples` etc.) are ignored and the model will be trained on the provided dataset instead. 
		</APIDescriptionSection>
	</div>
	<div className='code-container'>
		<div className='tabs-code'>
			<Tabs>
				<TabItem value='python' label='Python'>
```python
from artifex import Artifex

guardrail = Artifex().guardrail()

guardrail.train(
    unsafe_categories = {
        "violence": "Content describing or encouraging violent acts",
        "bullying": "Content involving harassment or intimidation of others",
        "misdemeanor": "Content involving minor criminal offenses",
        "vandalism": "Content involving deliberate destruction or damage to property"
    },
    language="english"
)
```
				</TabItem>
			</Tabs>
		</div>
		<div className='tabs-code'>
			<Tabs groupId="language">
				<TabItem value="response" label="Response">
```python
None
```
				</TabItem>
			</Tabs>		
		</div>
	</div>
</div>
