---
id: intro
hide_table_of_contents: true
---

import Link from '@docusaurus/Link';
import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

import { WEBSITE_BASE_URL, PLATFORM_BASE_URL } from '../consts';
import { Annex } from '../src/components/Annex';
import { ARTIFEX_AUTHENTICATION_URL, SYNTHEX_INTRO_URL } from '../consts';


# About Artifex

<div className='responsive-container'>
    <div className='text-container'>
        Artifex is an [open-source Python library](https://github.com/tanaos/artifex) developed 
        by <a href={WEBSITE_BASE_URL} target='_blank' rel='noopener'>Tanaos</a> for training small, private 
        AI models **without data**. How is it even possible, you might ask? Because synthetic training data
        is generated under the hood through our <Link to={SYNTHEX_INTRO_URL}>Synthex library</Link>. 
        
        All you need to do is select one of the AI models that are implemented by Artifex and describe **in natural 
        language** how the model should behave. Artifex will take care of the rest.
    </div>
    <div className='code-container'>
		<div className='tabs-code'>
			<Tabs>
				<TabItem value='python' label='Python'>
				```bash
                pip install --upgrade artifex
				```
				</TabItem>
			</Tabs>		
		</div>
    </div>
</div>